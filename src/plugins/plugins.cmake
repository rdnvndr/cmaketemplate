include(../../../main.cmake)

set(CMAKE_RUNTIME_OUTPUT_DIRECTORY ${APPPATH})
set(CMAKE_LIBRARY_OUTPUT_DIRECTORY ${RPLUGINPATH})
set(CMAKE_ARCHIVE_OUTPUT_DIRECTORY ${RPLUGINPATH})

add_library(${PROJECT_NAME} MODULE ${HEADERS} ${SOURCES} ${FORMS} ${RESOURCES})

target_include_directories(${PROJECT_NAME}
    PUBLIC ${PROJECT_SOURCE_DIR}/include
)
